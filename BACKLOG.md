# Недоделки

Живой список того, что заметили и не доделали. Правило: нашли хвост, оставили
его на потом — строка сюда сразу, а не в чат. Сделали — строку удаляем
(история остаётся в git). Крупные темы живут в своих документах (SEO.md,
VIDEO.md, DESIGN.md), здесь — только ссылка и что конкретно висит.

Формат пункта: что не так → чем грозит → что сделать. В скобках — дата,
когда заметили.

## Каталог и карточки

- **«Гребнево»: 101 кадр в первом экране вместо 50** (24.09). После снятия
  галереи (#27) вся `gallery` усадьбы ушла в рельс первого экрана: страница
  тяжелее, точки-индикаторы превращаются в пунктир. Урезать до 50,
  вертикальные вперёд, по кадрам от каждого из девяти залов.
- **Галерея на карточках снята, но не удалена** (23.09, #27). Код
  закомментирован в `ploshchadki/[slug].astro` и `podryadchiki/[category]/[slug].astro`,
  тип блока `gallery` в данных площадок живой. Решить: вернуть или вычистить.
- **Устаревший комментарий** в `podryadchiki/[category]/[slug].astro` (~стр. 76):
  «кадров ровно столько, сколько уходит в доску (PER_SPECIALIST)», а в рельс
  теперь идут все кадры.

## Видео (подробности — VIDEO.md)

- **Демо-ролики Лешаковых по 8 секунд** (24.09). 12 файлов `public/reels/*.mp4`
  (stairs, groom, morning…) не перегнаны до 60 с: исходников нет. Либо найти
  исходники, либо заменить работами реальных подрядчиков.
- **Вертикальный ролик на первом экране обрезан по высоте** (24.09). Файл
  кроится 9:16 → 2:3, окно `HeroStage` 4:5 с `cover` срезает ещё — видно
  ~70 % высоты исходника. По времени починили, по кадру нет.
- **Видео лежит в git** (24.09). После перехода на 60 с — около 110 МБ роликов
  в репозитории. При росте каталога выносить на CDN/сервер (см. VIDEO.md,
  «Долг: пережатие на стороне сервера»).
- **Подписи роликов «Из-за любви» сверены только по постеру** (24.09), не
  по всему ролику. У «Лесной Росы» подписи оказались перепутаны — здесь
  досмотреть полосой кадров.

## Подрядчики из реестра

- **Косарева (Reels-мейкер): нет контактов** в реестре (24.09).
- **Любовь Балашова (ведущая): нет материалов** — только ВК и Instagram.
  Нужен диск от заказчика.
- **Без портфолио и текста**: АРТ Невеста, Матрешка, Роман Каримов, Дарья
  Голева, LiRein, Студия Братьев Зиновеевых — в реестре черновиками.
  Актуальный список: `node scripts/registry-watch.mjs --all`.

## Отложено в правилах (не забыть, что это не сделано)

- Первый экран на десктопе — постоянное решение под широкий экран (AGENTS.md,
  «Пропорции фото»).
- Блог не переведён на кнопку «назад» (AGENTS.md, «Навигация»).
- Верхняя шапка `SiteHeader` выключена до конца дизайн-фазы.
- Ролики общего потока без привязки к карточке — перед продакшном привязать
  (AGENTS.md, «Видео»).
