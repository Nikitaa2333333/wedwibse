# SEO-схема проекта: блог → подборка → карточка

Внутренний документ. Здесь копятся находки по продвижению и решения,
которые фронт обязан не сломать. Обновлять при каждом новом разборе.

---

## 1. Разбор конкурента wedwed.ru (31.08.2026, Firecrawl)

### Что у них в карте сайта

1227 URL, три ветки:

| Ветка | URL | Кол-во |
|---|---|---|
| Каталог Москвы | `/catalog/...` | ~591 |
| Каталог СПб | `/spb/catalog/...` | ~245 |
| Блог | `/article/<рубрика>/<slug>/` | **382 статьи** |

Рубрики блога и объём: `podgotovka-k-svad-be` 104, `vdohnovenie` 97,
`otnosheniya` 41, `den-svad-by` 37, `obraz-nevesty` 28, `trendy` 26,
`podryadchiki` 23, `znamenitosti` 15, `stil-zhizni` 11.

Статьи опубликованы 10.2025, `article:modified_time` — 05.2026:
**старые тексты регулярно переобновляют** (свежесть в Яндексе).

### Как устроены их посадочные каталога

Четыре оси, под каждую комбинацию — отдельный URL:

- **тип**: лофт, ресторан/банкетный зал, шатёр, усадьба/особняк, веранда, коттедж, загородный клуб, яхт-клуб;
- **вместимость**: 10–15, 20–25, 30–35, 40–45, 50–70, 80–120; лофты на 100/200/300;
- **гео**: округа Москвы (ВАО, ЗАО, САО, ЦАО, ЮАО…), Подмосковье; в СПб — районы (Приморский, Курортный, Адмиралтейский, Ленобласть);
- **повод**: свадьба / корпоратив / новогодний корпоратив / день рождения / фуршет — весь набор осей дублируется под каждый повод.

Плюс фильтры подрядчиков (`veduschie/na_15_chelovek`, `fotografy/nedorogo`)
и полная копия дерева под `/spb`.

### Карта на каталоге площадок (скрин 03.09.2026)

Над сеткой карточек — Яндекс Карта (`api-maps.yandex.ru`) с кластеризацией:
близкие площадки на текущем зуме схлопываются в один красный кружок с числом
(сколько точек внутри), при увеличении зума кружки распадаются на одиночные
пины. Это штатный `ymaps.Clusterer` (или новый `ObjectManager`) — своя логика
кластеризации не писалась, перекрашен только сам значок под их алый.

Для нас это требует на момент подключения: (1) координаты каждой площадки —
геокодировать адрес один раз при заведении карточки (Яндекс Geocoder API),
хранить lat/lon в данных; (2) API-ключ Яндекс Карт для бизнеса (домен
привязывается); (3) сам скрипт карты — **новая клиентская зависимость**,
подключать только под страницу каталога, не глобально, и с обоснованием
по правилам CLAUDE.md. Смысла делать раньше реальных адресов подрядчиков
нет — сейчас в данных гео нет вообще.

### Главная находка

**Блог у них качает подборки-фильтры, а не карточки.** В теле статьи
6–10 контекстных ссылок точными анкорами, все — на страницы фильтров:

```
/spb/catalog/ploschadki/restoran-banketny-zal-dlya-svadbi/   анкор «банкетный зал»
/spb/catalog/ploschadki/usadba-osobnyak-dlya-svadby/         анкор «особняках»
/spb/catalog/ploschadki/restorany-kurortnyj/                 анкор «в Курортном районе»
/spb/catalog/ploschadki/loft-arenda/                         анкор «свадьбе в стиле лофт»
```

**А блок «WedWed рекомендует» под статьёй отдан подрядчикам** — ведущие,
фотографы, декор, кондитеры, стилисты. Площадок в нём нет даже под статьёй
целиком про выбор банкетного зала (проверено на двух статьях).
То есть конкретная площадка из блога не получает ничего.

**Связь односторонняя:** статья → фильтр. Карточка обратно на статьи не ссылается.

### Что из этого берём

| Их приём | Наше решение |
|---|---|
| 5–8 контекстных ссылок на подборки точными анкорами | обязательное правило статьи |
| Рубрика в URL, плоская структура | `/blog/<рубрика>/<slug>/` |
| Переобновление старых текстов | поля `published` / `updated` в данных статьи |
| Под статьёй — карточки подрядчиков | **у нас — карточки площадок** (их дыра) |
| Обратной связи нет | **у нас — блок «Читайте по теме» на карточке площадки** |

---

## 2. Наша схема перелинковки

```
блог (статья)
  ├─ 5–8 контекстных ссылок в тексте  →  подборка (посадочная-фильтр)
  ├─ блок «Площадки из этой статьи»   →  карточка площадки   ← перевес над конкурентом
  └─ блок «Читайте дальше»            →  соседние статьи

карточка площадки
  └─ блок «Читайте по теме»           →  статьи, где она упомянута   ← обратный вес
```

Носитель связи — данные, не вёрстка. В `src/data/articles.ts` у статьи два поля:

- `collections: { label, href }[]` — подборки, которые статья качает;
- `venues: string[]` — площадки, чьи карточки показываются под текстом.

Обратная выборка — `articlesAboutVenue(name)` в том же файле.

## 3. Типы статей, которые дают деньги

По убыванию коммерческого интента:

1. **«Ошибки при выборе X»** — прямой интент, ссылки на подборки естественны.
2. **«Свадьба на \<гео-объект\>»** (у конкурента — Финский залив). Гипотеза про
   Клязьму, Истру, Новую Ригу и округа Москвы **проверена Вордстатом 01.09.2026 и
   не подтвердилась**: 0–8 показов/мес. Гео-ось — «Москва» → «Подмосковье» →
   города МО (Красногорск, Мытищи, Одинцово, Химки). Подробно — [SEMANTICS.md](SEMANTICS.md).
3. **«Свадьба на N гостей»** — один-в-один мапится на подборки вместимости.
4. Чек-листы, бюджет, тайминг — трафиковые, денег почти не носят: держим меньшинством.

## 4. Что во фронте уже сделано (дизайн-прототип)

- `src/data/articles.ts` — тип статьи со связями `collections` / `venues`, инлайн-разметка (`[анкор](/url)`, `==маркер==`), 4 демо-текста.
- `src/components/ArticleVenues.astro` — блок «Площадки из этой статьи» + ряд подборок. Переиспользуемый: ляжет на посадочную подборки как «похожие площадки».
- `src/components/ArticleCard.astro` — плитка статьи (хаб, рубрика, «Читайте дальше»).
- `src/pages/blog/index.astro`, `/blog/[rubric]/index.astro`, `/blog/[rubric]/[slug].astro`.
- Ссылка «Блог» в `SiteHeader`.

## 5. Сайт закрыт от индексации (снять перед запуском!)

Пока идёт дизайн-фаза, сайт живёт на своём домене, но закрыт от поисковиков —
сырые страницы, попавшие в индекс, потом висят в выдаче месяцами и портят
первое впечатление о домене. Закрыто в три слоя, снимается одним флагом:

| Слой | Где | Что делает |
| --- | --- | --- |
| `<meta name="robots" content="noindex, nofollow">` | `src/layouts/Layout.astro` | закрывает все HTML-страницы |
| `robots.txt` → `Disallow: /` | `src/pages/robots.txt.ts` | просит роботов не обходить сайт |
| `X-Robots-Tag: noindex, nofollow` | конфиг nginx на VPS | закрывает и не-HTML (картинки, PDF) |

**Как открыть индексацию в день запуска:**

1. В воркфлоу деплоя выставить `SITE_INDEXABLE=true` — снимутся мета-тег и `Disallow`.
2. На сервере убрать строку `add_header X-Robots-Tag` из конфига nginx, `nginx -t && systemctl reload nginx`.
3. Только после этого — добавлять сайт в Яндекс.Вебмастер и Search Console
   (см. скилл `yandex-webmaster`), иначе получим «страница запрещена в robots.txt».

## 6. Wordstat API: чем снимаем частотность (разбор 01.09.2026)

Старого отдельного API Вордстата (`api.wordstat.yandex.net` + OAuth) больше нет —
Wordstat переехал в **Yandex Cloud Search API v2** (тот же ключ, что у AI Studio).
Источник истины — proto-спека, а не гипертекст-документация:
[`wordstat_service.proto`](https://github.com/yandex-cloud/cloudapi/blob/master/yandex/cloud/searchapi/v2/wordstat_service.proto).

Корень: `https://searchapi.api.cloud.yandex.net/v2/wordstat/`,
заголовок `Authorization: Api-Key <ключ>`, **`folderId` обязателен в теле каждого запроса.**

| Метод | Путь | Что даёт нам |
| --- | --- | --- |
| `GetTop` | `/topRequests` | `totalCount` фразы за 30 дней + до 2000 похожих запросов (`results`) + ассоциации (`associations`) |
| `GetDynamics` | `/dynamics` | ряд `{date, count, share}` по дням/неделям/месяцам — сезонность |
| `GetRegionsDistribution` | `/regions` | распределение по регионам/городам + `affinityIndex` |
| `GetRegionsTree` | `/getRegionsTree` | справочник ID регионов (кэшируем один раз) |

Ограничения из proto: `phrase` ≤ 400 символов, `numPhrases` 1–2000,
`regions` ≤ 100 ID, `devices` ≤ 3 (`DEVICE_ALL/DESKTOP/PHONE/TABLET`).
Грабли: `count` приходит **строкой** (int64 → JSON string); `period` только
с префиксом (`PERIOD_MONTHLY`); для monthly `toDate` обязан быть последним днём
месяца; операторов `!`, `+`, `[]` в API нет — точную частотность снимаем руками
в веб-Вордстате.

Обвязка: `scripts/wordstat.mjs` (Node 22, без зависимостей), затравки —
`scripts/wordstat-seed.txt`, выгрузки — `scripts/wordstat-out/`.
Доступы лежат в `.env` (`WORDSTAT_API_KEY`, `WORDSTAT_FOLDER_ID`), файл в `.gitignore`.

```
node scripts/wordstat.mjs batch scripts/wordstat-seed.txt --regions 213,1 --num 500
```

**Ассоциации смотреть обязательно:** там всплывает другое написание того же
интента. У нас так и вышло: «свадебная площадка» — 4 648 показов/мес, а «банкетный
зал» — 41 027. Термин интерфейса и термин поиска разошлись.

Разбор снятого ядра, оси подборок, сезонность и пул статей — **[SEMANTICS.md](SEMANTICS.md)**.
Пересобрать ядро: `node scripts/wordstat.mjs batch … && node scripts/wordstat-clusters.mjs core-msk`.

## 7. Кто стоит в выдаче по нашему ядру (снимок 18.09.2026)

Снято `scripts/serp.mjs` через тот же Yandex Cloud Search API (метод
`/v2/web/search`, ключи Wordstat): 97 запросов — топ-14 каждого кластера
ядра `core-msk`, регион 213, топ-10 органики. Сырьё —
`scripts/serp-out/serp-core-msk.csv`, отчёт — `serp-core-msk.md`.
Пересъёмка: `node scripts/serp.mjs run core-msk && node scripts/serp.mjs report core-msk`
(уже снятые запросы пропускаются — удалить csv для полной пересъёмки).
Это и есть наша метрика роста: доля голоса по ядру, раз в месяц.

### Доля голоса (вес = частота запроса × поправка на позицию)

| Домен | Доля | Что это | Чем ранжируется |
| --- | ---: | --- | --- |
| wedwed.ru | 51 % | агрегатор, 591 URL каталога Москвы | подборки тип × вместимость × гео |
| msk.gorko.ru | 30 % | агрегатор, 782 зала в Москве | подборки |
| weddywood.ru | 29 % | медиа | статьи (кластер info) |
| yandex.ru | 29 % | Карты, Картинки, Услуги | свои сервисы |
| avito.ru | 27 % | классифайд | объявления подрядчиков + журнал |
| alveowed.com | 20 % | агентство | страницы услуг «под ключ от X ₽», прайс |
| svadbanaprirode.com | 19 % | агрегатор, 1 848 URL одной глубины | подборки, тонкие |
| houseforwedding.ru | 13 % | **~30 страниц** | «Свадьба на 30 человек: разбор» — по одной странице на ось |

**Какие страницы стоят в топ-3:** подборки 62 %, карточки 22 %, статьи 10 %,
главные 7 %. Блог в этой нише — десятая часть выдачи; выдачу держат подборки.

### По кластерам — где замок крепкий, а где входная дверь

| Кластер | Показов | Кто держит | Замок |
| --- | ---: | --- | --- |
| vendors | 45 880 | avito 51 %, wedwed 39 %, кондитерские | крепкий: Авито с ценами в объявлениях |
| info | 28 361 | weddywood 62 %, yandex 58 %, wedding-magazine 56 % | медиа + нейроответы; не наш бой |
| core («площадки для свадьбы») | 19 187 | wedwed 82 %, gorko 80 %, kate-i-leo 38 % | крепкий, но листикл агентства «Топ-30 площадок» стоит третьим |
| venue-type | 17 899 | wedwed 73 %, gorko 60 %, restoclub 44 % | крепкий: сотни залов у каждого |
| **price** | 16 657 | alveowed 79 %, marmelad-wedding 50 % (**калькулятор**), avito, gazprombank | **слабый**: держат агентства и калькуляторы, агрегаторов нет |
| **geo** | 6 634 | wedwed 96 %, svadbanaprirode 70 %, wedding.mosreg.ru 49 % | средний: по городам МО стоит госсайт области — значит, никто не работает |
| **capacity** | 3 959 | svadbanaprirode 70 %, wedwed 48 %, boyarov 42 %, houseforwedding 40 %, darsy 34 % | **слабый**: агентства и сайт на 30 страниц |

Брендовые запросы площадок («долина», «дивный лес»): сайт площадки,
top15moscow, whitestudios, Яндекс Карты — карточка агрегатора в топ-5 есть,
ось рабочая.

### Что из этого следует — стратегия органического роста

1. **Подборки — единственная игра.** 62 % топ-3. Строим их как отдельные URL
   с h1, текстом-агрегатом из данных карточек и ценой в title: агентства
   выигрывают price/capacity страницами вида «Свадьба в Подмосковье под ключ —
   от 490 000 ₽». Цена в заголовке — признак ранжирования в этих кластерах.
2. **Порядок захода — от слабого замка к крепкому:**
   - **capacity** (10/20/30/50/100, камерная) — держат агентства и сайт
     на 30 страниц. Порог входа: 5–8 площадок под каждую цифру. Первая цель.
   - **price** — «сколько стоит свадьба» 5 683 стоит за калькулятором
     marmelad-wedding и прайсом alveowed. Нужен **калькулятор бюджета на
     наших данных** (аренда + меню × гости + 15 %), не статья. Вторая цель.
   - **geo по городам МО** — там ранжируется wedding.mosreg.ru: ось пустая.
     Подборки по 8–10 городам, как только под городом есть 3+ площадки.
   - **venue-type и core** — после 150–200 площадок в каталоге: раньше
     против 782 залов gorko не встать.
   - **vendors** — самый большой кластер, но против Авито; заходим карточками
     с открытой ценой (Авито выигрывает именно ценой в объявлении).
3. **Карточки** — под брендовый запрос `<имя> площадка для свадьбы`;
   работает уже сейчас, независимо от размера каталога.
4. **Блог — 10 % выдачи, и ранжируются только листиклы и прайсы**
   («Топ-30 площадок», «25 лучших мест» у kp.ru, «сколько стоит» у банка).
   Методические тексты в топ-3 не видны. Блог — после каталога, и в форме
   листиклов-хабов, когда есть что листить.
5. **Метрика** — доля голоса по ядру из `serp.mjs`, раз в месяц, плюс
   «Видимость в Алисе» в Вебмастере после открытия индексации.

Примечание: wedwed.ru и msk.gorko.ru sitemap с нашей сети не отдают
(соединение рвётся) — их структура снята Firecrawl'ом 31.08 (§1).

## 8. Долги (внутрянка, делаем после дизайна)

- [ ] **Настоящие URL подборок** `/moskva/ploshchadki/lofty/` со своим h1, текстом и открытой пагинацией. Сейчас `collectionHref()` в `articles.ts` временно отдаёт `/moskva/ploshchadki/?type=Лофт` — меняется одна функция.
- [ ] Каталог не читает query-параметры фильтра (ссылка на подборку открывает полный список).
- [ ] Блок «Читайте по теме» на карточке площадки (данные готовы: `articlesAboutVenue`).
- [ ] `sitemap.xml`, микроразметка `Article` / `BreadcrumbList`, `<link rel="prev/next">` на пагинации.
- [ ] Матрица «статья → подборки → площадки» на первые 20–30 текстов.
- [ ] Оси подборок под нашу Москву: тип × вместимость × округ/направление × повод.
- [ ] **Первый экран карточки печатает в HTML все кадры галереи** (19 у «Лесной Росы», виден один) — 16 КБ из 97 КБ страницы уходит на разбор того, чего на экране нет, и растёт вместе с каталогом. Разбор и решение — в `DESIGN.md`, «Первый экран карточки», раздел «Долг».
